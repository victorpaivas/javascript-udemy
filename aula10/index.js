//tipos de dados primitivos!
//STRING, NUMBER, UNDERFINED, NULL, BOOLEAN, SYMBOL

const nome ='Victor';  //string
const nome1 = "Victor";  //string
const nome2 = `Victor`;  //string
const num1 = 10; //number
const num2= 15.52;  //number com ponto flutuante
let nomeAluno; //underfined -> não aponta para local nenhum na memória
let sobrenomeAluno = null; // nulo -> não aponta para local nenhum da memória (usa-se quando quer desconfigurar uma variável)
const aprovado = true; // boolean = true or false (valor lógico) quer mudar o curso da aplicação

//console.log(typeof nomeAluno, nomeAluno)


